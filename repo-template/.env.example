# Generated by project-docs from the infrastructure doc.
# Copy to .env, populate, then run: ./scripts/sync-secrets.sh
#
# Every variable the project needs must be listed here. This file is the
# contract: sync-secrets.sh reads it to decide what to push to GitHub, and CI
# reads it to decide what to materialize back into .env on the runner.
# A variable missing here is a variable CI will not have.

# --- example shape; project-docs replaces this whole block ---
# DATABASE_URL=            # libSQL connection string
# DATABASE_AUTH_TOKEN=     # from the database dashboard
# APP_BASE_URL=            # http://localhost:5173 locally
